#include <bits/stdc++.h>

using namespace std;

const int mx = 1000;
int N, T, D;
int a[mx], t[mx], dp[mx][mx];

int main()
{
    cin >> N >> T >> D;
    
    for (int i = 1; i <= N; i++)
        cin >> a[i];
    
    for (int i = 1; i <= N; i++)
        cin >> t[i];

    for(int i=2; i<=N; i++){
        int tmp=0;
        for(int j=1; j<=D; j++){
            dp[i][k] =
        }
    }
}