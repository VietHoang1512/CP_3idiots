#include <bits/stdc++.h>
using namespace std;

int const mx = 1000;
int const mod = 1e9 + 7;

int N, M, a[mx];
int main()
{
    cin >> N >> M;
    for (int i=1;i<=N; i++){
        for(j=){
            for(int k=1; k*a[i]<j)
        }
    }
}