#include <bits/stdc++.h>
using namespace std;

class Solution
{
public:
    int maxValueAfterReverse(vector<int> &nums)
    {
        int total=nums.size();
        if(total==1) return 0;
        int l=-1, r=-1, mx=-INT_MAX, 
        for(int i=0; i<total-1; i++){
            for(int j=i+1; j< total; j++)
            int tmp=0;
                if(i>0) tmp
        }
    }
};