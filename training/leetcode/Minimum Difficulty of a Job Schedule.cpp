#include <bits/stdc++.h>
using namespace std;

// #define __builtin_popcount __builtin_popcountll

int main()
{
}

class Solution {
public:
    int minDifficulty(vector<int>& jobDifficulty, int d) {
        // sort(jobDifficulty.begin(), jobDifficulty.end());
        // int total = jobDifficulty.size();
        // if (total < d) return -1;
        // return jobDifficulty[total - 1]+accumulate(jobDifficulty.begin(), jobDifficulty.begin() + d-1, 0);
    }
};