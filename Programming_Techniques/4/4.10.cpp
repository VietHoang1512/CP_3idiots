#include <bits/stdc++.h>
using namespace std;

int N, Q;
vector<vector<string>> doc, query;
string s;

int main()
{
    cin >> N;
    for (int i = 0; i < N; i++)
    {
        cin >> s;
        vector<string> tokens;
        int curIndex = 0;

        doc.push_back(s);
    }

    cin >> Q;
    for (int i = 0; i < Q; i++)
    {
        cin >> s;
        query.push_back(s);
    }
}