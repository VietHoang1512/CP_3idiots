#include <bits/stdc++.h>

using namespace std;
int N, K1, K2;

int cons_day = 0;
int state[10000];
bool UCV(int i, int d)
{
    if (!d)
        return i;
    if (d<K1)
        return i==0;
    
    
}
int Try(int n, int d)
{
    if (d)
}

int main()
{
    cin >> N >> K1 >> K2;
    Try(1, 0);
}